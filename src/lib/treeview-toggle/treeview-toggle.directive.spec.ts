import { TreeviewToggleDirective } from './treeview-toggle.directive';

describe('TreeviewToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeviewToggleDirective();
    expect(directive).toBeTruthy();
  });
});
