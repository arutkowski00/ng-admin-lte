import { TreeviewMenuDirective } from './treeview-menu.directive';

describe('TreeviewMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeviewMenuDirective();
    expect(directive).toBeTruthy();
  });
});
